# Docs

Supplementary write-up material for the paper (dataset statistics, paper outline, drafting notes). Not required to run the code — see `case_study_spider_cot/` for the runnable pipeline.

The source markdown files currently live in `../../benchmark_related_code/` (`benchmark_dataset_info.md`, `paper_outline.md`) and have not been copied here yet — move or link them in before publishing to GitHub if you want them included in the repo.

## Experiment design drafts

- [`exp_design_l1l2l3_controlled_backbone.md`](exp_design_l1l2l3_controlled_backbone.md) — draft design for a controlled-backbone ablation (fixed LLM, varying only the L1/L2/L3 inference pipeline) to support the taxonomy claims in `survey.pdf` for EACL submission. Status: draft, not yet implemented.
